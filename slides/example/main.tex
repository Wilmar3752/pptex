% ──────────────────────────────────────────────────────────────────────────────
%  pptex — Example Presentation
%  Engine: XeLaTeX  |  Theme: Metropolis
%  Compile: pptex compile slides example
% ──────────────────────────────────────────────────────────────────────────────

\documentclass[
  aspectratio=169,
  12pt,
  xcolor={dvipsnames}
]{beamer}

% ── Theme ─────────────────────────────────────────────────────────────────────
\usetheme[
  progressbar=frametitle,
  block=fill,
]{metropolis}

% ── Packages ──────────────────────────────────────────────────────────────────
\usepackage{fontspec}
\usepackage{polyglossia}
  \setdefaultlanguage{spanish}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
  \usetikzlibrary{arrows.meta, positioning, shapes}

% ── Colors ────────────────────────────────────────────────────────────────────
\definecolor{pptexBlue}{HTML}{2B4590}
\definecolor{pptexAccent}{HTML}{E8433A}
\definecolor{pptexLight}{HTML}{F5F5F5}

\setbeamercolor{palette primary}{bg=pptexBlue}
\setbeamercolor{progress bar}{fg=pptexAccent}
\setbeamercolor{title separator}{fg=pptexAccent}
\setbeamercolor{alerted text}{fg=pptexAccent}

% ── Code listings ─────────────────────────────────────────────────────────────
\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{pptexBlue}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{pptexAccent},
  numbers=none,
  frame=single,
  breaklines=true,
  backgroundcolor=\color{pptexLight},
}

% ── Metadata ──────────────────────────────────────────────────────────────────
\title{Esta presentacion es toda vibecodeada}
\subtitle{pptex — plug \& play LaTeX con Docker}
\author{Wilmar Sepulveda \& Claude Code}
\institute{pptex}
\date{\today}

% ──────────────────────────────────────────────────────────────────────────────
\begin{document}

\input{slides/00_title}
\input{slides/01_instalacion}
\input{slides/02_uso}
\input{slides/03_prompt}

\end{document}
